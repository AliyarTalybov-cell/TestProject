## Прототип портала агронома

Этот репозиторий предназначен для фронтенд‑прототипа портала агронома.

## Запуск фронтенда

Фронтенд живёт в папке `frontend/`.

Требования: установленный Node.js (LTS).

```bash
cd frontend
npm install
npm run dev
```

## Страницы и маршруты

- **Авторизация**
  - Маршрут: `/login`
  - Назначение: вход пользователя по логину и паролю, базовая валидация, переход к дашборду после успешного входа.
- **Дашборд**
  - Маршрут: `/dashboard`
  - Назначение: сводка по хозяйству — количество полей, активные задачи, ближайшие работы, ключевые индикаторы состояния культур.
- **Поля**
  - Маршрут: `/fields`
  - Назначение: список/таблица полей с основными параметрами (название, культура, площадь, статус) и фильтрами.
- **Детали поля**
  - Маршрут: `/fields/:id`
  - Назначение: детальная информация по выбранному полю: культура, история работ, задачи, заметки, базовые показатели.
- **Задачи**
  - Маршрут: `/tasks`
  - Назначение: список задач агронома с фильтрами по статусу, дате, полю, приоритету.
- **Отчёты**
  - Маршрут: `/reports`
  - Назначение: набор отчётных экранов (урожайность по культурам, выполненные работы по периодам и т.п.).

## Выбор технологического стека

- **Основной фреймворк**: Vue 3
- **Сборщик/бандлер**: Vite
- **Язык**: TypeScript (приоритетно; при необходимости можно начать с JavaScript)
- **Состояние**: локальное состояние компонентов, при росте сложности — Pinia
- **Стили**: чистый CSS/SCSS, при необходимости можно добавить лёгкий UI‑кит (Naive UI, Element Plus и т.п.)

Таким образом, **Vue 3 + Vite** зафиксированы как базовый стек для реализации всего прототипа.

## Базовые модели данных

- **Поле (`Field`)**
  - `id`: уникальный идентификатор поля
  - `name`: название поля
  - `cropId`: ссылка на текущую культуру
  - `areaHa`: площадь в гектарах
  - `region`: регион/хозяйство
  - `status`: статус поля (например, `планируется`, `в работе`, `убрано`)
  - `lastInspectionDate`: дата последнего осмотра
  - `notes`: произвольные заметки
- **Культура (`Crop`)**
  - `id`: уникальный идентификатор культуры
  - `name`: название культуры (пшеница, кукуруза и т.п.)
  - `variety`: сорт/гибрид (опционально)
  - `season`: сезон/год (например, `2026`)
- **Задача (`Task`)**
  - `id`: уникальный идентификатор задачи
  - `fieldId`: ссылка на поле
  - `title`: краткое название задачи
  - `description`: подробное описание
  - `type`: тип задачи (обработка, осмотр, внесение удобрений и т.п.)
  - `status`: статус (`новая`, `в работе`, `завершена`, `отменена`)
  - `priority`: приоритет (`низкий`, `средний`, `высокий`)
  - `assignee`: ответственный (ФИО или роль)
  - `dueDate`: срок выполнения
  - `completedAt`: дата фактического завершения (опционально)
- **Отчёт/Сводка (`Report`)**
  - `id`: уникальный идентификатор отчёта
  - `type`: тип отчёта (урожайность по культурам, работы по периодам и т.п.)
  - `periodFrom` / `periodTo`: период, по которому строится отчёт
  - `fields`: набор полей/идентификаторов полей, попадающих в отчёт
  - `data`: агрегированные показатели (структура зависит от типа отчёта)

Связи:

- одно **поле** связано с одной актуальной **культурой** (`Field.cropId → Crop.id`);
- у одного **поля** может быть много **задач** (`Task.fieldId → Field.id`);
- **отчёты** агрегируют данные по нескольким полям, культурам и задачам.

## Дизайн‑референсы (HTML макеты)

В папке `design/` лежат HTML‑макеты. Они в основном на английском, но ниже зафиксировано, как мы их интерпретируем для русского прототипа.

- `design/design-cbe91594-bcce-4bb7-b0c5-2be206beafe9.html`
  - **Что это**: один файл с переключаемыми представлениями `login / dashboard / task / reports`.
  - **К чему маппим**:
    - `/login`: форма входа (в макете “Agronomist Login”, поля “Operator ID / Passcode”).
    - `/dashboard`: “Field Overview” + таблица полей + виджет прогноза погоды.
    - `/tasks` (пока как “создать задачу”): форма назначения задачи (сотрудник/поле/тип работ/объём).
    - `/reports`: круговая диаграмма причин простоев + таблица “план/факт” по сотрудникам.
  - **Русские термины**:
    - Dashboard → **Дашборд**
    - Create Task → **Создать задачу**
    - Downtime → **Простой**
    - Actual vs Plan → **План / факт**
- `design/design-3df49732-a9c5-4408-b32c-e5f72a87bb6a.html`
  - **Что это**: desktop‑layout с левым меню и “Field Operations”.
  - **К чему маппим**: `/dashboard` (сводка) + куски для `/fields` (таблица Active Fields) + блок “Assign New Task” как часть `/tasks`.
  - **Ключевые блоки**:
    - Active Fields (таблица поля/культура/площадь/статус)
    - Conditions (погода на 3 дня)
    - Assign New Task (назначение задачи оператору на поле)
    - Reports‑виджеты (причины простоев, план/факт по операторам)
- `design/design-3ab26ff7-e164-46ee-82e3-792a0242a022.html`
  - **Что это**: desktop‑layout “Operations Dashboard” с правой панелью “Create Task”.
  - **К чему маппим**: `/dashboard` (основная сетка) + “Create Task” как боковая форма для `/tasks` (или модальное окно в `/dashboard`).
  - **Русские пункты меню** (как ориентир для нашего UI):
    - Fields & Crops → **Поля и культуры**
    - Work Log → **Журнал работ**
    - Reports → **Отчёты**
    - Settings → **Настройки**
- `design/design-088a2a49-ea5b-4144-a18f-e3495c13d724.html`
  - **Что это**: мобильный экран “текущая задача на поле” + нижний лист причин простоя.
  - **К чему маппим**: сценарии задач (частично `/tasks/:id` в будущем) — “Старт работ” / “Отметить простой”.
- `design/design-753d077c-4aca-4ca0-a8f1-f3015e2b5a05.html`
  - **Что это**: мобильный экран назначения (field operator): локация/ID, текущая задача, детали (объём/техника), кнопки Start Work / Mark Downtime.
  - **К чему маппим**: сценарии задач (частично `/tasks/:id` в будущем).
- `design/design-4cc5f69c-b227-44fa-b2e9-811c6992d762.html`
  - **Что это**: ещё один мобильный “logbook/operator” экран текущего задания + модалка “Select Reason”.
  - **К чему маппим**: сценарии задач/журнала работ (частично будущий `/tasks/:id` или `/tasks` с действиями “старт/пауза/простой”).

